mixin hero(data)
    section.hero(class=data.type && '-'+data.type style="background-image: url(" + data.image + ")" )
        if (data.type !== 'small')
            .container.-narrow
                h1.hero_heading.h2= data.title
                if (data.type === 'large')
                    .hero_content.text-content !{data.content}
                a(href="#").hero_btn.btn-light= data.btn_text
